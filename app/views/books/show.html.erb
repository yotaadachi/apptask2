<div class="container">
  <div class="row">
   <div class="col-xs-3">
    <%= render 'users/userinfo', user: @user %>
    <%= render 'books/newbook', book: @book_new %>
   </div>
　　<div class="col-xs-9">
    <h2 class="headline" style="padding-top: 20px;">Book detail</h2>
    <table class="table">
     <thead>
     </thead>
     <tbody>
      <tr class="contents">
       <td>
         <%= link_to user_path(@user) do %>
            <%= attachment_image_tag @user, :profile_image, :fill, 60, 60, class: "img-circle pull-left profile-thumb", fallback: "no_image.jpg"%>
            <br><%= @user.name %></br>
           <% end %>
       </td>
　　　  <td><%= @book.title %></td>
　　　  <td><%= @book.body %></td>
       <td><%= link_to "Edit", edit_book_path, class: "btn btn-success" %></td>
       <td><%= link_to "Destroy", books_path, method: :delete, "data-confirm" => "Are you sure?", class: "btn btn-danger" %></td>
　　   </tr>
     </tbody>
    </table>
    <% if flash[:notice] %>
     <div class="flash">
     <%= flash[:notice] %>
    </div>
    <% end %>
  </div>
 </div>
</div>